# 計劃書

這個 repo 是 [neko-tensei](https://github.com/yazelin/neko-tensei) 漫畫產線的**入門樣板**。
目標讀者是漫畫產線實作營的學員：fork 下來、填自己的金鑰、按一次 Run workflow，
當晚就帶得走一頁真的漫畫。

**檔期未定（2026-08-11）**：原本排 8/19，但浮水印教學站可能先排，所以沒有時間壓力。

**進度：七步走完前四步。** 動手之前先讀完這份，尤其是每一步底下記的踩雷。

## 為什麼需要它

neko-tensei 本體 fork 起來只要改三個檔案（`story/cast.json`、`story/README.md`、
`episodes.json`），技術上不難。真正會卡住學員的是**角色設定圖**：這條產線的核心是
每個角色都要有一張穩定的參考圖，而那是創作，不是操作。學員在 90 分鐘裡從零生出一張
堪用的設定圖，機率不高，一卡住整晚就毀了。

所以樣板要預先附上一組**已經可用**的角色，讓「跑通產線」跟「創作自己的角色」拆成兩件事：

1. 課堂上：用預設角色出一頁，確認整條線是通的（成功經驗先拿到手）
2. 回家後：換成自己的角色

## 角色決策

**採用：格莉奇（Glitch）＋ 黑洞先生。**

### 格莉奇當主角

來源 [ai-brain-site](https://github.com/yazelin/ai-brain-site)。

**她跟黑洞先生是同一個晚上出生的。** 格莉奇本來是 2026-08-05 貼圖實作營簡報與講義
需要的示範角色（總不能每一頁換一隻），一張定裝照變九宮格、切開去背變八張貼圖，
整套流程就用她走完。後來送 LINE 審核時怕被要求補件、要證明角色有出處，才開始幫她
蓋網站，結果直接過審、補充資料完全沒用上，站就繼續做成了 glitch OS。

這件事讓這組搭檔比原本想的更順：兩個角色都是那場實作營的產物，樣板等於把那一晚
生出來的東西接著往下用。

選她當主角的理由：

| 條件 | 現況 |
|---|---|
| 素材 | 全身立繪（去背）、頭像，以及 **9 張貼圖等於 9 個現成表情姿勢** |
| 辨識道具 | 薄荷髮加呆毛、頭側兩個天線裝置、胸口 ERROR 牌、pixel glitch 方塊、choker、手腕裝置、彩虹透明吊帶 |
| 世界觀 | 已經有：4KB 記憶體設定、glitch OS、每日日記產線、Giscus 討論區、專屬單曲 |
| 畫風 | 動漫插畫，跟 neko-tensei 同一個家族 |

那 9 張貼圖是關鍵。這條產線最貴的成本是「同一個角色換表情不會漂」，而她的表情庫
已經畫好了，可以直接當參考圖用。

一魚多吃的部分：漫畫可以回頭嵌進 ai-brain-site 的日記頁，共用同一批讀者。

### 黑洞先生當搭檔

來源是 2026-08-05 貼圖實作營的現場示範角色（逐字稿裡出現 10 次，學員看著他誕生，
當天還做了「黑洞先生上班摸魚」那組貼圖）。設定是「黑洞的具現化」，禮帽、西裝、
星系頭、觸手腿。

配成一對的理由不只是湊人數：

- 格莉奇：記憶體只有 4KB，東西會**忘掉**
- 黑洞先生：黑洞的具現化，東西會**被吃掉**

兩個「東西會不見」的角色，機制不同，這是現成的喜劇引擎，不用另外編故事。

視覺上兩人差到極點（動漫少女對星系頭西裝觸手），樣板在課堂上不會出「認錯人」的意外。
「兩個長得像的角色怎麼分」那一課，用 neko-tensei 本體的兩隻虎斑貓當範例就好。

### 不採用阿蕊，理由不是畫風

阿蕊（[taiwan-people](https://github.com/yazelin/taiwan-people)）的參考圖其實是三個
候選裡最嚴謹的（`ref.png`、`ref-half.png`、`face.png` 四視圖，還附 `must_not` 清單）。
不用她是因為她的設定核心寫得很硬：

> 所以刻意**不設定**：家鄉、年齡、職業、身世、家庭。
> 一旦她有家鄉，其他二十一個縣市就都「不是她的」。

漫畫一定會給她這些。連載開始她就有職業、有朋友、有住的地方，「台灣人代表」的前提
當場垮掉。拿她當樣板等於**弄壞另一個專案**，這個成本不能付。

## 要做的東西

依序：

1. ~~**黑洞先生的角色設定圖**~~ **已完成**：`story/refs/blackhole.webp`，正面／側面／背面
   三視圖，白底無文字。產生它的 prompt 留在 `story/refs/blackhole.prompt.txt`，要重生或
   改設定時從那份改，不要重寫。

   鎖住的特徵（驗收時逐項對照）：星系球體頭、卡其禮帽加深棕帽帶、炭藍西裝加白襯衫、
   兩隻觸手臂從袖口伸出、**六隻觸手腿各穿一隻黑色短靴**、觸手內側整排吸盤。
   西裝褲只出現在正面與背面（見下方「接受的偏差」）。

   **目前是第三版，三個視圖都是六隻腳**（用 `count_limbs.py --views 3 --expect 6`
   機械驗過，不是用眼睛看的）。

   側視這一個視角總共生了七次，記錄如下。三個屬性（腿數、姿勢、褲管）**每次只中兩個**：

   | 次 | 腿數 | 姿勢 | 褲管 |
   |---|---|---|---|
   | 三視圖一起生 v1 | 4 | — | 無 |
   | 三視圖一起生 v2 | 5 | — | 有 |
   | 單獨生側視 1 | 6 | 走路 | 有 |
   | 單獨生側視 2 | 7 | 站定 | 有 |
   | 單獨生側視 3 | 7 | 站定 | 有 |
   | **單獨生側視 4（採用）** | **6** | **站定** | 無 |
   | 拿 4 去改、只叫它加褲管 | 5 | 站定 | 有 |

   兩個真的有效的做法：

   - **只單獨生側視那一張，再合成三視圖。** 整張一起生時模型要同時處理三個視角的
     遮擋關係，側視永遠是被犧牲的那個。失敗時也只要重生一張。
   - **把「不穩定」交給機械驗收加自動重試**，不要靠把 prompt 寫得越來越兇。
     `count_limbs.py` 回傳 exit code，寫成迴圈生到過為止就好。實測改 prompt 的寫法
     沒有讓它變穩，重試才有用。

   一個沒效的做法：**「照抄這張、只改一件事」在這個案例失敗了。** 這招在洗格莉奇的
   設定圖時有效，但叫它加褲管時腿數從六掉到五。差別大概在於前者是後製處理、
   後者仍然要模型重畫。

   **接受的偏差：側視沒有西裝褲。** 正面與背面都有，設計本身有記錄。追七次換不到
   三項全中，而側視在設定圖裡的職責是交代側面剪影與比例，那兩件是對的。
   之後真的需要側面帶褲管時再單獨處理。

   有效的那句 prompt 存在 `story/refs/blackhole-side.prompt.txt`：六隻腿放射狀長在
   身體周圍所以從側面落在六個不同深度、每隻靴都不可被完全遮擋、而且要站定不准走路
   （六隻靴全部平貼地面承重、身體不前傾、沒有一隻往前擺）。

   兩個教訓：

   - 驗收要用機械的方式逐項走完。第一版是驗了正面背面就推論側面也對，漏掉的那個
     缺陷是 yazelin 自己看出來的，不是驗收流程抓到的。
   - **生圖後端會掛住，不是慢。** 第二版第一次跑了七個半小時沒有回應，第一版只花四分鐘。
     之後所有生圖呼叫都要加 `timeout`。另外，判斷後端死活先送一個「畫灰方塊」的最小任務，
     不要憑錯誤訊息猜。
2. ~~**格莉奇的角色設定圖**~~ **已完成**：`story/refs/glitch.webp`（三視圖）與
   `story/refs/glitch-front.webp`（單張正面立繪）。

   **不是重生的，是把 8/5 實作營原本就畫好的那兩張洗乾淨。** 原圖底是均勻的青色
   （RGB 50,181,211、飽和度 0.76），角色身上最接近的薄荷色只有 0.19，用「色相 172-208
   加飽和度大於 0.35」的遮罩就分得開；再只保留與畫布邊界相連的區域，被角色包住的
   青色小面板才不會被誤削。頂部三個視角標籤是孤立色塊、不會向下連到頭部，用這個性質
   機械地刮掉，不用手切矩形。右下角的 logo 另外處理。

   重生會有漂掉的風險，洗原圖沒有——這是這一步該用洗的而不是用生的理由。

3. ~~**`style_ref`**~~ **已完成**：`story/refs/style.webp`，產生它的 prompt 在
   `story/refs/style.prompt.txt`。

   **這一格回答了卡住整個計劃的問題：兩個角色合得起來，黑洞先生不用重畫。**
   把 3D 玩具質感的他和平塗動漫的格莉奇一起丟進去，模型自己把他拉進了她的畫風，
   兩人讀起來就是同一部作品。所以這部作品的畫風定為**平塗賽璐璐動漫**。

   測試設計刻意留白的三件事，都是為了讓結果可判讀：不放對話框與文字（這次只看畫風，
   加了字會引進錯字與指錯人這類另一種錯誤）、給明確光源（同一道光是綁住不同質感角色
   最有效的手段）、兩個角色的辨識特徵都寫到字面含否定句。

   驗收結果：畫風統一、格莉奇的天線髮夾 ERROR 補丁 choker 手環吊帶球鞋像素腮紅
   glitch 方塊全部保留。

   **但腿的數量沒有鎖住：這一格畫成了七隻。** 設定圖的側視是五隻、規格寫的是六隻、
   成品是七隻——三個數字互不相同。所以「把數量寫進文字就鎖得住」是錯的，
   `must_not` 裡那句「不多於也不少於六隻」對這個模型無效。

   這件事有兩個後果，都要處理：

   - 設定圖側視少一隻那個缺陷**必須修**，不能像先前判斷的那樣放著。參考圖是模型
     唯一真正會照抄的東西，文字管不動它。
   - **可數的數量必須列入每頁的強制驗收項**，而且不能靠人眼。細長、重疊、深色的
     東西人眼數不準——這一頁我數成六隻，是 yazelin 看出來是七隻的，而且這已經是
     同一類錯誤的第三次。要用 neko-tensei 那套 vision 驗收去問「畫面裡有幾隻腳」。

   一個踩到的坑：第一次生成逾時，原因是我丟了兩張原尺寸 PNG（各 2 MB）當參考圖，
   違反 cast-lock 的「參考圖要先縮圖」。縮成 1024px JPEG（150 KB）後一次就過。
   neko-tensei 目前沒事（它的參考圖都是 webp），但產線裡沒有東西擋著有人放大檔進去。
4. ~~**`story/cast.json`**~~ **已完成**。用 cast-lock 的 `build.py --refs-only` 與
   `check.py` 都跑過，路徑解得開、驗收清單長得出來（格莉奇 13 項、黑洞先生 13 項）。

   `balloon_ref`（七種對話框圖表）整份沿用 neko-tensei 的，已複製進來。

   **`world` 已經有第一個場景**：`room`（廢棄機房裡的小屋），`story/refs/room.webp`，
   產生它的 prompt 在 `story/refs/room.prompt.txt`。一次就過，空景、平塗、地板中間留空、
   斷電機櫃剛好卡在書桌與床之間，一眼說明「溫馨的角落在廢棄機房裡」。

   來源是 `ai-brain-site` 的桌布（那本來就是她房間的既定視覺），但**不能直接拿來用**：
   那張是厚塗、只有一個視角、而且裝不下六隻腳的黑洞先生。所以拿它當道具與配色的來源，
   畫風錨另外給 `style_ref`，重畫成更寬的空景。

   **刻意不做平面配置圖。** cast-lock 講得很死：一個機位一張空景圖，共用一張標著
   A/B/C 的平面圖模型會認錯，而且沒有空景圖的機位等於不存在。所以額外的機位要等
   分鏡真的需要哪些鏡頭才生，不要預先造一堆用不到的。

   `style_ref` 的 `desc` 裡明文註記「這張圖的腿數是錯的（七隻），它只是畫風參考、
   永遠不是造型參考」，免得之後有人拿它當對照。

   `global_rules` 裡有兩條是這次踩出來的：黑洞先生的腿數是最容易錯的東西（設定圖
   曾經五隻、規格六隻、成品七隻，三個數字都不一樣），以及數量不准用眼睛驗。
5. ~~**`story/README.md`**~~ **已完成**：世界觀、共生機制、四個可重複的笑點模板、
   以及一份「不要出現的東西」清單。

   **核心設定：讀者就是格莉奇的記憶體。** 她只有 4KB 存不下東西，所以把記憶外包給
   讀者，網站是她的外接硬碟。這條讓三件事同時成立：她為什麼經營一個網站、為什麼在意
   留言、以及這部漫畫為什麼連載在她自己的站上。**連載平台變成劇情的一部分**，不只是
   發表管道。

   結構是三方而不是兩人：黑洞先生負責刪除、格莉奇是 4KB 轉接器、讀者負責寫入。

   **不給任何角色創傷背景。** 格莉奇為什麼只有 4KB 的官方說法是「她自己也忘記了」，
   這句本身就是笑點，不是迴避。這條是硬規定，寫進了「不要出現的東西」。
6. **`episodes.json`**：第一話的骨架。
7. ~~**README**~~ **已完成**：fork 指南、四個 secret、兩個一定會踩的坑寫在最前面、
   cast-lock 三條規矩摘要、驗收怎麼做。`tools/` 也補上了（`build.py`、`check.py`、
   `count_limbs.py`），附上游來源以便同步。授權分開處理：程式碼 MIT、角色素材 CC BY-NC。

8. **把產線與網站搬過來**（尚未做，README 已註明產線還沒接上）。

   **發表點決定了：漫畫發表在這個 repo 自己的 GitHub Pages 上**，`ai-brain-site`
   之後用自訂 app 的 iframe 內嵌進來（那個功能還在做）。所以是**一個發表點加一扇窗**，
   不是兩個發表點。樣板要附完整網站，學員 fork 完跑一次就拿到一個能分享的網址。

   一個要記得的坑：兩邊都在 `yazelin.github.io` 底下，**同源代表共用 localStorage**。
   漫畫站的閱讀進度和格莉奇OS 的聊天記錄會撞在同一個 storage，key 要先講好前綴。
   Service Worker 那邊沒事，scope 是按路徑分的。

   搬運範圍分兩階段做，中間有一個可驗證的檢查點：

   **階段一：產線（能出圖就算過）**

   - `scripts/next_episode.py`（1382 行）、`prompt.py`、`verify_pages.py` 與三支測試
   - `.github/workflows/next-episode.yml`
   - **6 處寫死 `yazelin/neko-tensei` 要參數化**：GraphQL 查詢的
     `repository(owner:..., name:...)`、`UA` 字串、PR 內文組圖片網址的兩處
     `raw.githubusercontent.com`、`GEMINI_BASE` 與 `IMG_BASE` 的預設值
     （後兩個已經寫進 README 的「一定會踩的坑」）

   **階段二：網站（Pages 開得起來就算過）**

   - `build.py`（網站產生器，從 `episodes.json` 生閱讀頁、首頁列表、sitemap、
     `sw.js` 快取清單）
   - 站殼：`index.html`、`style.css`、`app.js`、`sw.js`、`manifest.json`、`robots.txt`
   - `partials/footer.html`

   **這兩樣要重做，不是複製得來的**：`assets/` 的 favicon、PWA 圖示與 og 圖，
   以及 `char/*.html` 角色介紹頁（neko-tensei 有四隻貓，我們要寫格莉奇與黑洞先生）。
   giscus 的討論區分類 id 也要在新 repo 重開。

## cast-lock 怎麼進來

[cast-lock](https://github.com/yazelin/cast-lock-skill) 是獨立的 skill repo，neko-tensei 是用
`cast.json` 的 `_` 欄位指過去，不複製。但學員是 fork，他們機器上沒有裝那個 skill，
而課程第四段整段就是驗收。所以拆開處理：

| 東西 | 進不進 | 為什麼 |
|---|---|---|
| `build.py`／`check.py` | 進，放 `tools/` | 沒有它第四段跑不了；setup 腳本放消費 repo 內是既有慣例 |
| `cast.json` | 進 | 本來就要有 |
| SKILL.md 全文 | 不進 | 複製一份等於多一個會漂的事實來源 |
| README 裡的三條摘要 | 進 | 設定圖一定要當參考圖傳、特徵寫到字面、「沒有什麼」也要寫死；後面附連結指向 cast-lock repo |

## fork 者一定會踩的兩個坑

從 neko-tensei README 抄過來，樣板的 README 要寫在最前面：

- `GEMINI_WEB_BASE_URL` 預設指向私有中繼站，**必須**改成
  `https://generativelanguage.googleapis.com`
- `IMAGE_PROVIDER` 預設是 `codex`（私有服務），**必須**改成 `gemini` 並給
  `GEMINI_IMAGE_KEY`

另外 `OPENAI_MAX_TOKENS` 預設 32768，這是最常見的陷阱。

## 課程流程

先給成功經驗，再教原理，再讓學員自己來。

| 段 | 內容 | 時間 |
|---|---|---|
| 課前 | fork 樣板、填金鑰、跑一次 `--plan-only` 確認通了 | 學員自己 |
| 一 | 用預設角色**先出一頁**，看著它動 | 15 分 |
| 二 | 角色設定怎麼寫才不會漂 | 35 分 |
| 三 | 換成你的角色，再出一頁 | 25 分 |
| 四 | 驗收：逐格對照劇本找縫 | 15 分 |

前置條件維持原報名頁寫的那條：電腦上有 Claude Code 或 Codex CLI 這類 AI 編碼工具，
而且用過至少一次。有 Agent 簡化的不是操作，是**除錯**（金鑰貼錯、JSON 少一個逗號、
workflow 紅了看不懂），沒有的話一個人卡住就要等講師。

## 待拍板

- 樣板要不要順便當成格莉奇的正式連載起點（如果要，`episodes.json` 的第一話就不能
  只是範例，要能接下去）
- repo 現在是 private，內容做完再轉 public 並設成 template

## 授權

依既有政策分開處理：

- 程式碼：MIT，林亞澤
- 角色素材（格莉奇、黑洞先生）：CC BY-NC
